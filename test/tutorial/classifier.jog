mod classifier
use nn

fn main(
  x: tensor<f32, [1, 4]>,
  weight: tensor<f32, [4, 3]>,
  bias: tensor<f32, [3]>
) -> tensor<f32, [1, 3]> {
  let logits = nn.linear(x, weight, bias)
  return nn.softmax(logits, 1, 1.0)
}
