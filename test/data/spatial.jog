module model
use nn
use tensor

[entry]
fn main(
  x: tensor<f32, [1, 1, 3, 3]>,
  weight: tensor<f32, [1, 1, 2, 2]>
) -> tensor<f32, [1, 1, 2, 2]> {
  return nn.conv2d(x, weight, [1, 1], [0, 0, 0, 0], [1, 1], 1)
}
