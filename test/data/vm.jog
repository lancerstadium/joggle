module vm_case
use base

fn main(x: i64, y: i64, flag: bool) -> i64 {
  var value = x + y
  if flag {
    value *= 2
  } else {
    value -= 1
  }
  return value
}

fn shift(x: i64, amount: i64) -> i64 {
  return x >> amount
}

fn divide(x: i64, y: i64) -> i64 {
  return x / y
}
