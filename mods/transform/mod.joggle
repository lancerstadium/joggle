joggle 1;

mod transform@1.2.0 {
  fn inline(input: fn) -> fn;
  fn replace(input: fn, before: fn, after: fn) -> fn;
  fn replace(input: mod, before: fn, after: fn) -> mod;
  fn resolve(input: mod) -> mod;
}
