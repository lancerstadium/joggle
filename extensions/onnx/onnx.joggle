joggle 1;

module onnx@1.0.0 {
  import nn@2.0.0;
  import tensor@2.0.0;

  type resources();

  fn read(input: bytes) -> (module, resources);
  fn lookup(input: resources, resource: string) -> bytes;
}
