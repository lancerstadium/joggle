joggle 1;

mod external@1.0.0 {
  type scalar(bits: int);

  fn make<N: int>(bits: N) -> scalar<N>;
  fn keep<T>(input: T) -> T;
  fn converted<T>(input: T) -> T;
  fn convert(input: fn) -> fn;
  fn main(input: scalar<8>) -> scalar<8> {
    output = keep(input);
    return output;
  }
}
