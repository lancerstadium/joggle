module sat.c
use c
use sat

fn prepare(m: Mod) -> bool {
  var changed = sat.materialize(
    m, [8, 16, 32, 63], [ty("i8"), ty("i16"), ty("i32"), ty("i64")]
  )
  changed = c.prepare(m) || changed
  return changed
}
