module sat.vm
use sat
use vm

fn lower(m: Mod) -> bool {
  return sat.materialize(m, [63], [ty("i64")])
}

fn prepare(m: Mod) -> bool {
  var changed = lower(m)
  changed = vm.prepare(m) || changed
  return changed
}
