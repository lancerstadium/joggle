{
  "status": "DRAFT - not yet frozen; freeze before the pilot, then never edit",
  "agent": {
    "backend": "ollama",
    "model": "qwen3.5:9b",
    "options": {"temperature": 0, "seed": 42, "think": false},
    "step_cap": 24
  },
  "runs_per_task_per_arm": 5,
  "arms": {
    "A": {"name": "malleable", "workdir": "scratch copy of a Joggle module directory",
          "tools": ["read_file", "write_file", "run: joggle check", "run: joggle query", "run: joggle emit"]},
    "B": {"name": "maintained", "workdir": "scratch copy of the same decision in a separately maintained implementation",
          "tools": ["read_file", "write_file", "run: build"]}
  },
  "tasks": [
    {
      "id": "T1-storage-ranking",
      "kind": "real",
      "statement": "Change how the storage planner orders candidate slots so that, when two candidates score equally, the one leaving more capacity available for later values is chosen first. The change must not alter which values are eligible for a slot.",
      "success_A": "joggle check accepts the edited module AND the plan produced for the study subject differs in the intended direction",
      "success_B": "the rebuilt compiler compiles AND its plan for the same subject passes the same oracle",
      "frozen": false,
      "note": "Mirrors the manuscript's own storage-planner edit, so the task is known to be achievable by hand."
    },
    {
      "id": "T2-prepare-cleanup",
      "kind": "real",
      "statement": "Make the C preparation step also fold branches whose condition is provably true from the available interval information, leaving every other cleanup step in place.",
      "success_A": "joggle check accepts the edited module AND the emitted C for the study subject loses at least one branch",
      "success_B": "the rebuilt compiler emits C for the same subject that loses at least one branch",
      "frozen": false,
      "note": "Mirrors the second derivation case in the manuscript."
    },
    {
      "id": "C1-control-tile-size",
      "kind": "control",
      "statement": "Change the tile width used when the plan is emitted from 4 to 8, leaving everything else unchanged.",
      "success_A": "joggle check accepts the edited module AND the emitted C reflects the new width",
      "success_B": "the rebuilt compiler emits C reflecting the new width",
      "frozen": false,
      "note": "Requires locating one constant, not reasoning about a decision. If both arms fail this, the harness or the model is the problem, not the mechanism."
    }
  ]
}
