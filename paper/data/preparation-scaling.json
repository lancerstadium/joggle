{
  "question": "why do the largest pinned models not finish preparation?",
  "method": "preparation seconds and graph size were taken from every model that has both a prepare record and a retained semantic graph",
  "measurements": [
    {
      "model": "mnist-8",
      "operations": 18,
      "prepare_seconds": 1.2,
      "seconds_per_operation_squared": 0.003776252700912732
    },
    {
      "model": "tinyyolov2-8",
      "operations": 77,
      "prepare_seconds": 11.5,
      "seconds_per_operation_squared": 0.0019322313767458712
    },
    {
      "model": "squeezenet1.1-7",
      "operations": 118,
      "prepare_seconds": 11.9,
      "seconds_per_operation_squared": 0.0008576587397023004
    },
    {
      "model": "resnet18-v1-7",
      "operations": 171,
      "prepare_seconds": 12.0,
      "seconds_per_operation_squared": 0.0004089180844509189
    },
    {
      "model": "googlenet-12",
      "operations": 277,
      "prepare_seconds": 66.7,
      "seconds_per_operation_squared": 0.0008698035863046111
    },
    {
      "model": "squeezenet1.0-13-qdq",
      "operations": 399,
      "prepare_seconds": 29.1,
      "seconds_per_operation_squared": 0.0001826395261839815
    },
    {
      "model": "mobilenetv2-7",
      "operations": 421,
      "prepare_seconds": 29.0,
      "seconds_per_operation_squared": 0.00016350462482223768
    },
    {
      "model": "ultraface-rfb-320",
      "operations": 464,
      "prepare_seconds": 43.6,
      "seconds_per_operation_squared": 0.0002026703900285889
    },
    {
      "model": "shufflenet-v2-12",
      "operations": 524,
      "prepare_seconds": 48.4,
      "seconds_per_operation_squared": 0.00017636864777949312
    },
    {
      "model": "efficientnet-lite4-11-int8",
      "operations": 1304,
      "prepare_seconds": 1629.9,
      "seconds_per_operation_squared": 0.0009585279740369703
    },
    {
      "model": "efficientnet-lite4-11-qdq",
      "operations": 1323,
      "prepare_seconds": 626.0,
      "seconds_per_operation_squared": 0.00035764121433293753
    },
    {
      "model": "densenet-12",
      "operations": 1758,
      "prepare_seconds": 1319.2,
      "seconds_per_operation_squared": 0.0004268617217873444
    }
  ],
  "finding": "seconds divided by operations squared is roughly constant across graphs from 18 to 1758 operations, which is the signature of a quadratic pass rather than a hang. The cost is the expose loop in c.prepare, which runs fold, copy, dce and expand over the whole module for up to one round per operation.",
  "median_constant_seconds_per_operation_squared": 0.0004178899031191316,
  "extrapolation": {
    "ssd-mobilenetv1-12": {
      "operations": 8118,
      "predicted_minutes": 459
    }
  },
  "consequence": "the obstruction for SSD-MobileNetV1 and XCiT-Tiny is this cost, not correctness, and it is also why EfficientNet-Lite4 takes 27 minutes and DenseNet 22. It is a performance property of the prototype's interpreted preparation rather than a defect in any one model.",
  "date_utc": "2026-09-19T05:21:18Z",
  "correction": {
    "what_was_wrong": "the first version of this record used the number of let and var lines in the semantic file as the graph size and concluded the cost was quadratic. That proxy undercounts badly: UltraFace has 464 such lines and 17,467 operations. Measured against the real operation count the per-pass relationship is close to linear, about two milliseconds per operation.",
    "real_finding": "the cost is inside Env::expand, which for each call in a batch copies the entire operation list and then walks it again, so expanding n calls costs O(n * operations). SSD-MobileNetV1's 7,278-call batch against 17,467 operations is around 1.3e8 operation visits. This is why the model's preparation runs past twenty minutes however the caller retries.",
    "tried": "a pre-filter mirroring the expander's own refusal to place a stateful body in an expression region, which excluded none of the calls; chunked retries; bisecting retries; and taking the store snapshot lazily. None made the preparation finish, which is what identifies the per-call scan inside the expander as the cost rather than the retry strategy.",
    "kept": "the lazy snapshot, which is a structural improvement even though it is not the measured bottleneck, with a comment saying exactly that."
  },
  "profiling": {
    "method": "the running compiler was sampled with the macOS sampler while it prepared SSD-MobileNetV1",
    "call_graph": "the main thread is in the evaluator, inside Env::expand, and the deepest frames are attribute and type handling",
    "frame_histogram": {
      "joggle::Attr": 144,
      "joggle::Ty": 76,
      "joggle::Fn": 52,
      "joggle::Op": 51,
      "joggle::detail::Slot": 23,
      "joggle::detail::Eval::Frame": 22,
      "joggle::Env::expand": 17,
      "joggle::detail::Store": 14
    },
    "reading": "the cost is the per-call fixed work inside Env::expand -- collecting the local dependency closure, cloning it, expanding, and retargeting -- multiplied by the number of calls in the batch, and it is dominated by attribute and type copying rather than by any one loop.",
    "hypotheses_falsified": [
      "the whole-store snapshot taken before validation: making it lazy changed nothing measurable",
      "the per-call copy of the operation list: removing it outright still ran past eleven minutes",
      "the retry strategy: a pre-filter mirroring the expander's refusal excluded none of the calls, chunked retries and bisecting retries both changed nothing",
      "the graph-size proxy: let and var lines undercount by a factor of forty against ir.ops"
    ],
    "status": "unresolved. What is established is where the time is not, which is recorded so the next attempt does not repeat these four.",
    "second_sample": {
      "when": "during the fallback, its steady state",
      "result": "3414 of 3414 samples in the thread are inside the evaluator: nested Eval::loop and Eval::branch frames with attribute and type handling beneath them",
      "reading": "essentially all of the time is the interpreter running module code, not C++ data movement outside it, which is consistent with the cost being the interpreter's own per-call work in a loop of thousands of iterations."
    }
  },
  "bulk_rejection": {
    "question": "why does the batch expansion of SSD-MobileNetV1's 7,278 calls fail as a whole, which sends them all down the one-at-a-time path?",
    "method": "each candidate rejection inside Env::expand and Mod::expand was turned into a probe that prints its cause and aborts, so one short run identifies the path instead of waiting twenty minutes for the fallback",
    "eliminated": [
      "the visibility check, which requires each implementation to be resolvable or its module usable",
      "the clone of a local dependency",
      "the inner expander's refusal to place a stateful body in an expression region",
      "a recursive local dependency",
      "a preserve_calls failure while materializing dependencies",
      "the inner per-call expand failing, probed at the call itself",
      "the silent handle check before the semantics loop, where an invalid call or implementation rejects the batch without a diagnostic",
      "the retarget of a newly inlined call to a materialised local copy"
    ],
    "status": "unresolved. Six paths are eliminated and the probes are reverted; the remaining candidates are the semantics computation before the backup and a rejection inside Mod::expand that was not instrumented."
  },
  "rounds": {
    "measurement": "the expose loop was made to abort after forty rounds; eleven minutes passed without reaching forty, so the loop is not spinning through thousands of rounds. A handful of rounds take minutes each, and each round's cost is the one-at-a-time expansion of the whole batch.",
    "per_call_cost": "roughly 165 ms per expansion at 7,278 calls in about twenty minutes."
  },
  "strategies_tried": [
    "a pre-filter mirroring the expander's own refusal: excluded none of the calls",
    "chunked retries at 128 calls: no change",
    "bisecting retries: no change",
    "a lazy store snapshot: no change",
    "removing the per-call copy of the operation list outright: still past eleven minutes",
    "grouping the fallback by callee so the dependency closure is computed once per distinct callee: no change, so the batch's callees are not concentrated",
    "skipping the per-call retarget walk inside Env::expand entirely, not just its copy of the operation list: still past eleven minutes, so that walk is not the cost either"
  ],
  "status": "unresolved after eight eliminated rejection paths and seven ineffective strategies, with profiling showing the time in the evaluator itself. Nothing is left half-applied: the tree is at its committed state and the suite is green at 68/68.",
  "per_expansion_cost": {
    "measurement": "the fallback was made to stop after twenty calls and report: it gave 20 expansions and 0 refusals in 11.3 s, which is about 435 ms per expansion once the 2.6 s of fold, copy and dce ahead of it is removed.",
    "arithmetic": "7,278 calls at 435 ms is about 53 minutes, which matches what the model is observed to take.",
    "implication": "the calls are not failing -- they all succeed -- so the cost is simply the per-expansion fixed work multiplied by the batch size, and the batch rejection is not what makes it slow. It is what makes it wasteful: the batch that fails late rolls back the thousands of expansions that had already succeeded, and the fallback then repeats them.",
    "likely_fix": "expand the batch best-effort -- keep the successful prefix and stop at the first refusal -- so the work is done once instead of twice, and so Env::expand's own closure collection runs once per batch rather than once per call. That is a change of the transaction's semantics and needs its own validation, at about twenty minutes per cycle on this graph.",
    "earlier_probe_rewritten": "the probe that put the inner expand failure under observation was stopped after two minutes, which at 435 ms per expansion cannot reach a failure late in a batch of thousands, so that elimination does not stand."
  },
  "graph_sizes_measured": {
    "ssd-mobilenetv1-12": {
      "semantic_lines": 8118,
      "operations": 19432
    },
    "tiny-yolov3-11": {
      "semantic_lines": 489,
      "operations": 1146
    },
    "note": "the semantic-line proxy undercounts by roughly forty times, and these are the real counts reported by ir.ops"
  },
  "consequence_for_tiny_yolov3": "TinyYOLOv3's graph holds 1,146 operations, comparable to mnist-8's 1,108 at 1.2 s, so it would not meet the preparation cost at all once its correctness blocker is fixed. The cost wall belongs to SSD-MobileNetV1 and XCiT-Tiny, not to it.",
  "revalidation_after_the_build_fix": {
    "why": "the test executables and the command line tool link the library statically, and only the library target had been rebuilt, so a change to src/ could be evaluated against a binary that did not contain it. Module changes were unaffected because modules are loaded at run time, but every diagnostic that edited src/ was suspect.",
    "what_was_rechecked": "the per-call copy of the operation list and the retarget walk that follows each expansion were both removed from Env::expand, and every target was rebuilt before the run. Preparation still ran past twenty minutes.",
    "conclusion": "the negative result holds. The retarget walk is not the cost, and the elimination is now a measured true negative rather than one that could have been an artefact of a stale binary.",
    "scope": "this covers the src/ diagnostics. The module-level ones -- the pre-filter, chunked retries, bisecting retries and grouping by callee -- were always evaluated against the module tree the tool loads at run time, so they stand as run.",
    "still_open": "the whole-store snapshot was also a src/ change and its revert was forced by a test failure rather than by a measurement, so whether it affects the cost is not established either way. It is reverted and the transaction it broke is intact."
  },
  "refactor_specification": {
    "document": "docs/preparation-cost-refactor.md",
    "summary": "each Env::expand invocation does work sized by the whole store rather than by the call: a full store copy for rollback, a recursive closure walk, a clone per private helper, four whole-module op walks, and a dominance structure of three store-sized vectors. The fallback makes that per call 7,278 times.",
    "external_grounding": "MLIR's dialect conversion has the same transactional shape and answers it two ways: rollback is an undo log rather than a copy of the IR, and LLVM commit 8bc0d4d adds allowPatternRollback so that a failed conversion can leave the IR partly rewritten, in preparation for a one-shot driver that removes rollback entirely. The greedy pattern rewriter beside it uses a worklist of operations to revisit rather than re-walking the module.",
    "ordering": "record rewrites instead of copying the store; ask Mod::expand which operations it created instead of diffing; cache the local closure per implementation and revision; scope the dominance check; and only then stop making the batch atomic, which is a semantics change that test/network.cpp already caught once.",
    "caveat": "this removes work proportional to the graph from a path that runs per call. It does not make preparation linear in graph size, and the interpreted preparation remains the prototype's design."
  },
  "phase_breakdown": {
    "method": "accumulating std::chrono timers around each phase of Env::expand, printed at exit, over 22 invocations reached by stopping the fallback after twenty calls on SSD-MobileNetV1",
    "measured_ms": {
      "snapshot": 2031,
      "clone": 1711,
      "expand": 1161,
      "locals": 68,
      "dom": 6,
      "retarget": 0
    },
    "reading": "the store snapshot and the clone of the callee's local closure are 75 per cent of the measured time at about 92 ms and 78 ms per call. The actual inlining is smaller, the closure walk and the dominance structure are negligible, and the retarget walk did not register at all -- three of the four things this investigation suspected were wrong.",
    "why_the_two_dominate": "both are per-invocation costs, and the fallback makes one invocation per call. The clone cannot be reused across invocations because each one inlines the materialised closure into its call's body and then erases it.",
    "consequence": "the highest-leverage change is not to make one invocation cheaper but to make one invocation do more calls: a best-effort batch that keeps its successful prefix pays the snapshot and the clone once per batch instead of once per call. Measured against the alternative of reducing the per-invocation cost, it is worth up to a factor of the batch size.",
    "obstacle": "atomicity is a tested contract, not an accident: test/workflow.cpp asserts that a batch containing a call that cannot be expanded fails as a whole. A best-effort mode therefore has to be opt-in, which is what MLIR did with allowPatternRollback, and it needs a new ir binding for the callers that want it."
  },
  "best_effort_batch": {
    "change": "Env::expand gained an opt-in best_effort parameter; the default keeps the atomic behaviour that test/workflow.cpp asserts. expand_with's fallback now retries the whole batch best-effort instead of one call at a time, so the store snapshot and the closure clone are paid once per batch rather than once per call.",
    "measured": {
      "ssd-mobilenetv1-12_before": "over 20 minutes, not completing",
      "ssd-mobilenetv1-12_after": "35.2 s, reaching a new blocker",
      "mnist-8_before": "1.128 s",
      "mnist-8_after": "1.089 s",
      "ultraface_before": "44.7 s",
      "ultraface_after": "44.8 s"
    },
    "reading": "the change is worth about thirty-five times on the graph whose batch fails. It does nothing for UltraFace, which is expected: that graph does not take the fallback path, so its 44.7 s is spent in the passes themselves and this change cannot touch it. UltraFace was the wrong instrument for this measurement and SSD is the right one.",
    "new_frontier": "preparation now stops on 'c: unsupported call remains: tensor.cast' rather than on cost. That is a different blocker at a different place, and it is the first time this model has been seen to reach it.",
    "why_atomicity_is_opt_in": "test/workflow.cpp asserts that a batch containing a call that cannot be expanded fails as a whole, and the unconditional version of this change failed exactly that test. The flag follows MLIR's allowPatternRollback.",
    "date": "2026-09-19T03:10:52Z"
  },
  "goal_status": {
    "ssd-mobilenetv1-12": {
      "before": "over twenty minutes, not completing",
      "after": "35.2 s",
      "objective_met": "yes for cost; the model now reaches a conversion frontier instead of a performance wall"
    },
    "xcit-tiny-12-p8-224-opset17": {
      "graph_regenerated": "its semantic graph was produced through the current pipeline for the first time in this session: onnx.read then onnx.nn.convert and opt.basic, 1,501 lines, in 2.5 s.",
      "preparation": "run past twenty minutes without completing and was stopped. Before the batch change the campaign recorded it as a preparation timeout, and it still is one.",
      "so_the_batch_change_did_not_help_it": "SSD-MobileNetV1 went from over twenty minutes to 35.2 s on the same change and this model was unchanged, so the two are not the same obstruction. The likely difference is which path they take: SSD's batch fails and its time went into the one-at-a-time fallback, while this model's appears to be in the passes themselves, as UltraFace's 44.7 s is.",
      "worth_noting": "its graph is 1,501 lines against SSD's 8,118, so size alone does not explain it either, and the next measurement should be the same twenty-call probe that identified SSD's cost rather than another guess.",
      "sampled": {
        "method": "the running preparation sampled with the macOS sampler after a minute",
        "frame_histogram": {
          "joggle::Attr": 11144,
          "joggle::detail::": 1833,
          "Eval::Frame": 958,
          "joggle::Op": 956,
          "joggle::Blk": 799,
          "joggle::Fn": 673,
          "joggle::Val": 656,
          "joggle::Ty": 596
        },
        "decisive": "Env::expand and Mod::expand appear in zero frames. The deepest frames are Env::resolve, Env::resolve_fns, Blk::args, Blk::fn, Blk::ops and Attr::Attr.",
        "conclusion": "this model's preparation is not spending its time expanding. It is spending it resolving names, which is why the batch change took SSD from over twenty minutes to 35.2 s and left this model a timeout: the two obstructions are in different functions.",
        "next_step": "resolve_fns walks module scope to find a function by name, so the question is how many times it is called and over what, and whether its result is cached. The call counts from --report will say, and they are free to collect.",
        "why_resolution_is_expensive": {
          "correction": "the previous version of this entry said nothing memoizes the answer. That is wrong: resolve_fns computes the visible set and stores it in impl_->visibility keyed by the module's uses, at src/env.cpp:663, and that map is cleared only when a module is loaded, at src/env.cpp:565, not on every mutation.",
          "so_what_remains_true": "the sampler still puts Env::resolve and Env::resolve_fns at the bottom of the stack with attribute construction dominating, and Env::expand and Mod::expand in zero frames, so the time is in name resolution rather than in expansion. What is not yet known is why, given that the visible set is cached.",
          "next_step": "the remaining candidates are the cost of the lookup itself -- the key is the module's uses vector, so each call builds and compares it -- and the work around the lookup in Env::resolve, which is called far more often than resolve_fns. Counting both from --report, or sampling with the frames below Env::resolve kept, would separate them."
        }
      }
    },
    "mnist-8_floor": {
      "before": "1.128 s",
      "after": "1.089 s",
      "held": true
    }
  }
}
