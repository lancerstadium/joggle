title: Joggle
description: One typed language for model semantics, compiler transformations, and artifact emission.
markdown: kramdown
repository: lancerstadium/joggle
permalink: pretty
plugins:
  - jekyll-relative-links
relative_links:
  enabled: true
  collections: true
defaults:
  - scope:
      path: ""
    values:
      layout: default
