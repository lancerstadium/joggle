joggle 1;

module fixed@1.0.0 {
  import bitmath@1 as math;

  interface codec: type {
    encode(value: f64) -> i64;
    decode(bits: i64) -> f64;
  }

  type q(width: i64, fraction: i64, signed: bool = true) : math.numeric_format, codec;
}
