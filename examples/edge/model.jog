module model
use edge
use tensor

[entry]
fn main(
  a: tensor<f32, [2, 3]>, b: tensor<f32, [3, 2]>
) -> tensor<f32, [2, 2]> {
  return edge.matmul(a, b)
}

[entry]
fn extrema(x: tensor<f32, [4]>) -> (f32, f32) {
  let low, high = edge.extrema(x)
  return low, high
}
