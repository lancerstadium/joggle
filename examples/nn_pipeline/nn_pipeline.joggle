joggle 1;

module nn_pipeline@2.0.0 {
  import example_accel@2.0.0;
  import nn@2.0.0;

  fn convert_relu(input: module) -> module;
  fn count_ops(input: module) -> int;
  fn emit_joggle(input: module) -> bytes;
  fn prepare(input: module, use_accel: bool) -> module {
    if use_accel {
      return convert_relu(input);
    }
    return input;
  }
  fn compile(input: module, use_accel: bool) -> bytes {
    return emit_joggle(prepare(input, use_accel));
  }
}
